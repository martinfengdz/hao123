# 奇易智能导航 · 发布说明（GitHub Release）

> 本文件用于 GitHub Release 的正文。发布时把下面内容贴到 Release 描述即可。

## V3.3.01 更新（2026-08-21）· 新增「计算机工具页」出厂自建页面 + 底部快捷访问

按版本号约定 A.B.CC，本版为架构内功能级小版本（3.2.34 → 3.3.01）。

- 应需求「`\\192.168.1.9\工程软件开发\计算机` 目录下全部文件加入默认自建页面 + 加入底部快捷访问」：
  - **出厂自建页面新增 6 个工具页**（随默认页面机制播种到数据卷 `pages/`，开机自动就位）：
    - 多功能计算器 `calculator.html`
    - 齿轮参数计算器 `calculator2.html`
    - 材料价格计算器 `Material.html`（配套 `prices.js` 行情文件，由 `update_prices.py` 生成/更新）
    - 智能税务计算器 `customs.html`、理财计算器 `zlcalculator.html` 覆盖为用户本地最新版
  - **底部快捷访问新增 3 项**：多功能计算器、齿轮参数、材料价格；**升级合并机制 `mergeQuickAccess`**：以 href 为键，老用户已有自定义项原样保留，出厂新增项自动追加到尾部（实测：用户自定义项 + 3 新项并存，17 项）。
  - **自建页面机制扩展支持 `.js` 资源**：播种白名单、`safePageName`、`serveUserPage` Content-Type 三处同步放开 `.js`（`application/javascript`），使 `Material.html` 可加载相对路径 `prices.js`。
- 版本号同步：server.js VERSION / script.js VERSION / admin.js APP_VERSION / package.json / manifest（version=3.3.01，移除非官方 `arch` 字段，以 `platform=all` 覆盖）/ Dockerfile label / 全部本地 css/js `?v=`。
- 修复与加固：`mergeSite` 对 quickAccess 由整组替换改为按 href 合并，升级不丢用户配置。


## V3.2.33 更新（2026-08-20）· 页首高度压缩 + 日期行追加「第N周」

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.32 → 3.2.33）。

- 应需求「严格按图排版 + 高度压缩 + 上面有两个'周'信息（'周X' + '第N周'）」：
  - **高度压缩**：页首整体 `.header-main` min-height `130px→104px`、LOGO `.col-left .site-logo img` max-height `110px→88px`、中列 `.col-center` min-height `90px→64px`、`date-stack` gap `4px→2px` 并加 `line-height:1.25`、日期字号 `#current-time` `1.4rem→1.18rem`/`#lunar-date` `.95rem→.88rem`、按钮 min-height `36px→30px`/字号 `.9rem→.85rem`；768px 响应式 LOGO 进一步缩到 70×150。整体高度与图一致（紧凑），蓝边页首约 100-110px。
  - **「周X + 第N周」两个周信息**：`script.js` 新增 `getISOWeek(date)`（ISO 8601 周编号，周一首、第 1 周含 1 月 4 日），`updateTimeDisplay` 在 `#current-time` 末尾追加「 `周X`  `第N周` 」（例如「2026-08-20 20:28:15 周四 第34周」）。
- 页首三列结构（`.col-left` LOGO+日期·农历·版权 / `.col-center` 天气+浮层 / `.col-right` 三按钮）+ 问候浮层 `top:50% translate(-50%,-50%)` 居中盖天气 + 0.5s 弹性滑入 + 30s 进度条 + 30s 自动关闭 + 悬停暂停 + 可 ✕ 关闭 —— **完全保留**（V3.2.32 已对，结构未动）。
- 天气仍在红框下面（中列 `.col-center`），问候浮层在天气上居中盖住，30 秒后自动消失露天气——与 V3.2.32 行为一致。
- 内置天气组件（wttr.in）、`weatherCity`、`weatherCode` 自定义覆盖、搜索引擎默认百度、开幕动画、免密登录等能力保持不变；全部本地 css/js `?v=` 升级到 3.2.33。

## V3.2.32 更新（2026-08-20）· 页首改回横向 3 列：LOGO+日期 / 天气+浮层 / 三按钮

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.31 → 3.2.32）。

- 应需求（截图）将页首改回 V3.2.26 风格的横向 3 列布局，LOGO（小人）仍在最左：
  - 左列 `.col-left`：LOGO（`.site-logo` max-height:110px 垂直铺满列高）+ `.date-stack` 竖向 3 行（行1 `#current-time` 秒级日期 + 中文「周X」、行2 `#lunar-date` 农历「干支年 生肖年 农历月日」、行3 `.brand-copy` 版权年份 + 🔒无痕）。
  - 中列 `.col-center`：`#weather-area` 天气组件（内置 wttr.in 或后台自定义 iframe）；问候浮层在 `.col-center` 内 absolute `top:50% translate(-50%,-50%)` **居中盖住天气组件**，30s 自动关闭后天气露出来。
  - 右列 `.col-right`：主题 / 主页 / 刷新 三按钮**横排居中**。
- script.js `updateTimeDisplay` 在 `#current-time` 文本末尾追加中文星期（如「周四」），日期颜色时段逻辑不变。
- 问候浮层行为完全保留：0.5s 弹性滑入（cubic-bezier 回弹）、30s 自动关闭、悬停暂停（.paused）、可手动 ✕ 关闭、渐变深蓝背景、底部 30s 倒计时进度条。
- 弃用 `.header-logo`/`.header-body`/`.header-row`/`.date-row`/`.mid-row`/`.mid-left`/`.weather-wrap`/`.mid-buttons`/`.copyright-row`/`.header-datetime`，新增 `.col-left`/`.col-center`/`.col-right`/`.date-stack`；问候浮层定位上下文改为 `.col-center`。
- 内置天气组件（wttr.in）、`weatherCity`、`weatherCode` 自定义覆盖、搜索引擎默认百度、开幕动画、免密登录等能力保持不变；全部本地 css/js `?v=` 升级到 3.2.32。

## V3.2.31 更新（2026-08-20）· 页首三行重排：行1 日期+农历 / 行2 站点名+天气并排+三按钮 / 行3 版权

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.30 → 3.2.31）。

- 应需求（ASCII 布局图）重排页首三行，LOGO（小人）仍占三行最左：
  - 行1 `.date-row`（顶部）：秒级日期 + 农历（干支年 生肖年 农历月日）**并排居中**（原行3 的日期+农历上移到行1）。
  - 行2 `.mid-row`（**中间行**）：左块 `.mid-left` = 站点名「奇易智能导航」（左）+ 天气组件（右）**并排**；问候浮层仍在天气组件内 absolute `top:50% translate(-50%,-50%)` **居中盖住天气**，30s 自动关闭后天气露出来；右块 `.mid-buttons` = 主题 / 主页 / 刷新 三按钮**横排居中**。
  - 行3 `.copyright-row`（底部）：版权年份 + 🔒无痕（居中）。
- 问候浮层行为完全保留：0.5s 弹性滑入（cubic-bezier 回弹）、30s 自动关闭、悬停暂停（.paused）、可手动 ✕ 关闭、渐变深蓝背景、底部 30s 倒计时进度条。
- 弃用 `.weather-row`/`.buttons-row`/`.meta-row`，新增 `.date-row`/`.mid-row`/`.mid-left`/`.weather-wrap`/`.mid-buttons`/`.copyright-row`；问候浮层定位上下文从 `.weather-row` 改为 `.weather-wrap`（行2 天气组件）。
- 内置天气组件（wttr.in）、`weatherCity`、`weatherCode` 自定义覆盖、搜索引擎默认百度、开幕动画、免密登录等能力保持不变；全部本地 css/js `?v=` 升级到 3.2.31。

## V3.2.30 更新（2026-08-20）· 页首重排：LOGO 占三行最左 + 三按钮横排中间行

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.29 → 3.2.30）。

- 应需求「LOGO（小人）占三行，在最左边，主题 / 主页 / 刷新 三按钮横排，在中间行」，页首重构：
  - `.header-main` 改 flex `align-items: stretch`：左侧 `.header-logo`（LOGO 大图）竖向占满三行高度；右侧 `.header-body` 为三行内容（flex column，三行均分）。
  - 行1 `.weather-row`（顶部）：天气组件（实时温度 + 城市 + 风力湿度 + 3 天预报）；问候浮层移入该行内，absolute `top:50% translate(-50%,-50%)` 居中盖住天气。
  - 行2 `.buttons-row`（**中间行**）：主题 / 主页 / 刷新 三按钮**横排居中**。
  - 行3 `.meta-row`（底部）：站点名「奇易智能导航」+ 版权年份与 🔒无痕 + 实时日期时间（秒级）+ 农历（干支年 生肖年 农历月日）。
- 问候浮层行为完全保留：0.5s 弹性滑入（cubic-bezier 回弹）、30s 自动关闭、悬停暂停（.paused）、可手动 ✕ 关闭、渐变深蓝背景、底部 30s 倒计时进度条。
- 弃用 `.header-left`/`.header-center`/`.header-right` 三列类，新增 `.header-logo`/`.header-body`/`.header-row`/`.weather-row`/`.buttons-row`/`.meta-row`。
- 内置天气组件（wttr.in）、`weatherCity`、`weatherCode` 自定义覆盖、搜索引擎默认百度、开幕动画、免密登录等能力保持不变；全部本地 css/js `?v=` 升级到 3.2.30。

## V3.2.29 更新（2026-08-20）· 问候浮层居中盖在天气组件（30s 自动关闭后露天气）

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.28 → 3.2.29）。

- 3.2.28 的 `top:8px` 让问候浮层贴在 .header 顶部（仅在天气顶部**上方相邻**，没真正盖住天气组件），用户截图复盘后指出"问候浮层**把天气盖住**"应理解为浮层覆盖天气显示。
- 3.2.29 调整定位：`.greeting-container` 改为 `top: 50%; transform: translate(-50%, -50%)`，垂直水平居中于 `.header`——正好居中盖在天气组件中央。
- 关键帧 `greetingSlideIn`：起点 `translate(-50%, calc(-50% - 60px))`（从上方 60px 处滑入），终点 `translate(-50%, -50%)`（居中于天气）。
- 30 秒自动关闭后，天气组件从下方完整露出来；其他行为（0.5s 滑入、悬停暂停、可 ✕、渐变深蓝、底部进度条、弹性 cubic-bezier 回弹）全部保留。
- 页首排版完全不变（左 LOGO+站点名+版权/无痕+日期+农历 / 中天气 / 右三按钮）。

## V3.2.28 更新（2026-08-20）· 问候浮层回滚为覆盖式（盖住天气顶部，30s 自动关闭后露天气）

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.27 → 3.2.28）。

- 问候语横幅 `.greeting-container` 回滚为覆盖式定位（应需求「按 V3.2.19 页首排版不要变，天气组件在红色框位，问候语横幅浮层在天气预报组件正上方，**把天气盖住**，问候有时间到自动关闭」）：
  - 从 `.header-center` 顶部悬停式（3.2.27 浮在天气上方不挡）改回 `.header` 容器内绝对定位（`left:50% top:8px translateX(-50%)`）。
  - 水平居中于页首，正好落在中列（天气组件）的正上方，**盖住天气顶部**。
  - 问候浮层 30 秒自动关闭后，天气组件（实时温度 + 城市 + 风力湿度 + 3 天预报）从下方完整露出来。
  - 移除 `.header-center` 上的 `position: relative`（不再作为问候浮层定位上下文）。
- 问候浮层行为完全保留（无功能改动）：
  - 0.5 秒从页首顶部弹性滑入（`@keyframes greetingSlideIn` 终点 `translate(-50%, 0)`，cubic-bezier 回弹）。
  - 渐变深蓝背景（`#1e3c72 → #667eea`）+ 阴影卡片。
  - 底部 30 秒倒计时进度条（`.greeting-progress`）。
  - 30 秒后自动关闭；悬停暂停（`.paused`），移出恢复；可手动 ✕ 关闭。
- 页首排版保持不变（左列 LOGO + 站点名 + 版权/无痕 + 实时日期时间 + 农历 / 中列天气组件 / 右列主题·主页·刷新三按钮）；内置 wttr.in 天气组件、`weatherCity` 设置、`weatherCode` 自定义覆盖能力保留；全部本地 css/js `?v=` 升级到 3.2.28。

## V3.2.27 更新（2026-08-20）· 问候浮层重新定位（悬停在天气组件正上方）

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.26 → 3.2.27）。

- 问候语横幅 `.greeting-container` 重新定位：
  - 从 `.header` 容器（盖在天气上方）移入 `.header-center` 内，作为 `#weather-area` 的兄弟元素。
  - 定位上下文改为 `.header-center`（`position: relative`），绝对定位 `top: -8px; transform: translate(-50%, -100%)`，从而**悬停在天气预报组件正上方居中**，不再遮挡天气内容。
  - 中列红色框位完全留给天气预报组件（实时温度 + 城市 + 风力湿度 + 3 天预报），问候浮层在天气的正上方。
- 问候语横幅行为完全保留（无功能改动）：
  - 加载 0.5 秒后从顶部弹性滑入（`@keyframes greetingSlideIn`，cubic-bezier 回弹）。
  - 渐变深蓝背景（`#1e3c72 → #667eea`）+ 阴影卡片。
  - 底部 30 秒倒计时进度条（`.greeting-progress`，`@keyframes greetingProgress`）。
  - 30 秒后自动关闭；悬停暂停（`.paused`），移出恢复；可手动 ✕ 关闭。
- 其他保持不变：左列 LOGO + 站点名 + 版权/无痕 + 实时日期时间（秒级）+ 农历；右列主题/主页/刷新三按钮；内置天气组件（wttr.in 免密钥）；搜索引擎默认百度；开幕动画；免密登录；全部本地 css/js `?v=` 升级到 3.2.27。

## V3.2.26 更新（2026-08-20）· 页首三大块 + 内置天气组件 + 问候横幅增强

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.25 → 3.2.26）。

- 页首恢复「三大块」布局（V3.2.19 风格：左 LOGO+日期农历、右按钮），新增中列天气：
  - 左列 `.header-left`：LOGO（`.site-logo`）+ 站点名「奇易智能导航」（`.brand-name`）+ 版权年份与无痕标识（`.brand-copy`）+ 实时日期时间（`#current-time`，秒级更新）+ 农历（`#lunar-date`，干支年 生肖年 农历月日，如「丙午年 马年 七月初七」）。
  - 中列 `.header-center`：天气预报组件（`#weather-area`）。内置 wttr.in 免密钥组件（实时温度 + 城市 + 天气描述 + 风力 + 湿度 + 3 天预报），城市默认「广州」，后台「外观」可改；若在后台填了自定义天气嵌入代码（如中国天气网 iframe）则优先使用自定义代码。
  - 右列 `.header-right`：主题（`#toggle-theme`）/ 主页（`#set-homepage`）/ 刷新（`#refresh-page`）三个按钮。
  - 出厂默认 `weatherCode` 置空 → 默认启用内置天气组件（原中国天气网 iframe 天气条作为后台可选代码保留，可随时填回）。
- 问候语横幅（`.greeting-container`）增强：
  - 页面加载 0.5 秒后从顶部弹性滑入（`@keyframes greetingSlideIn`，cubic-bezier 回弹）。
  - 渐变色背景 + 弹性动画，悬浮于页首上方居中。
  - 底部新增 30 秒倒计时进度条（`.greeting-progress`，width 100%→0，`@keyframes greetingProgress`）。
  - 30 秒后自动关闭；鼠标悬停暂停计时与进度条，移出后恢复；也可手动点 ✕ 关闭（当天不再显示）。
- 新增设置：`weatherCity`（天气城市，默认「广州」，后台「外观-首页行为」可改）。
- 农历、搜索引擎默认百度、开幕动画、免密登录、用户名/密码列等能力保持不变；全部本地 css/js `?v=` 升级到 3.2.26。

## V3.2.25 更新（2026-08-20）· 页首三行三列网格 + 问候浮层

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.24 → 3.2.25）。

- 页首重构为「三行三列」网格（`.header-main` display:grid，3 列 × 3 行）：
  - 左格 `.header-left`（竖向三行）：行1 = LOGO（`.site-logo`）；行2 = 站点名「奇易智能导航」（`.brand-name`）；行3 = 版权年份（© <span id="current-year">）+ 无痕标识（`.incognito-badge`）。
  - 中格 `.header-center`（跨三行垂直居中）：天气预报（`.weather-area` / `#weather-area`），后台「外观」可配置嵌入代码，留空隐藏。
  - 右格 `.header-right`（跨三行、竖向右对齐）：主题（`#toggle-theme`）/ 主页（`#set-homepage`）/ 刷新（`#refresh-page`）三个按钮。
  - 问候语 `.greeting-container` 改为绝对定位浮层（`.header` 定位上下文，`left:50% top:8px translateX(-50%)`），浮于页首上方居中、覆盖天气（中格）正上方；关闭/自动隐藏/悬停暂停逻辑不变。
  - 弃用 `.header-brand`/`.header-body`/`.header-greeting`/`.header-weather`/`.header-meta`，新增 `.header-left`/`.header-center`/`.header-right`。
- LOGO 不再跨整行，回到左格三行内（与站点名、版权同属左列）。农历「干支年 生肖年 农历月日」、出厂默认中国天气网天气条、搜索引擎默认百度、开幕动画、免密登录、用户名/密码列等能力保持不变；全部本地 css/js `?v=` 升级到 3.2.25。

## V3.2.24 更新（2026-08-20）· LOGO 占三行 + 问候/天气重排

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.23 → 3.2.24）。

- 页首布局调整（应需求「LOGO 位置不变、仍占三行，日期等按 3.2.20，天气加在问候正下方，问候浮在正上方」）：
  - LOGO 回到页首左侧，竖向占满三行高度（`.header-main` 用 `align-items:stretch`，`.header-brand` 整块居中占满），恢复 3.2.20 的左侧大 LOGO 风格。
  - 右侧三行内容重排：行1 = 问候语（浮于顶部，正常流浮层 pill，去绝对定位避免重叠）；行2 = 天气预报（紧接问候正下方、左对齐）；行3 = 日期（公历+农历，左）+ 主题·主页·刷新按钮（右）。
  - 问候语由 3.2.23 的「绝对定位浮于天气上方」改为顶部一行浮层，关闭/自动隐藏逻辑不变；天气条与问候同列左对齐，满足「天气就在问候正下方」。
  - 弃用 `.header-row-brand`/`.header-row-mid`/`.mid-date`/`.mid-weather`/`.mid-tools`，新增 `.header-main`/`.header-brand`/`.header-body`/`.header-greeting`/`.header-weather`/`.header-meta`/`.time-display`/`.header-tools`。
- 农历「干支年 生肖年 农历月日」、出厂默认中国天气网天气条、搜索引擎默认百度、开幕动画、免密登录、用户名/密码列等能力保持不变；全部本地 css/js `?v=` 升级到 3.2.24。

## V3.2.23 更新（2026-08-20）· 页首固定三行布局

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.22 → 3.2.23）。

- **页首固定三行布局**：行1 品牌（LOGO + 站点名 + 版权）；行2（中间行）三栏并排——左=日期（公历+农历）、中=天气预报、右=主题 / 主页 / 刷新按钮。
- **问候语改为居中浮层**：由独立行改为绝对定位浮层，浮于天气（中间行）正上方居中，加载后短暂出现、可手动关闭，不再占用独立行高。
- 农历「干支年 生肖年 农历月日」与出厂默认天气条保持不变；全部本地 css/js `?v=` 升级到 3.2.23。

## V3.2.22 更新（2026-08-19）· 农历干支生肖显示 + 天气条并入日期行

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.21 → 3.2.22）。

- **农历日期显示为「干支年 + 生肖年 + 农历月日」**：首页日期行的农历显示为「丙午年 马年 七月初七」格式（天干地支年、生肖、农历月日一体显示，节日自动加粗标色、悬停提示节日名）。
- **天气条并入日期行（不再单独占一行）**：天气区由页首独立整行改为与 LOGO + 公历/农历日期**同一行并排显示**；窄屏放不下时由 `header-brand` 折行兜底，不会溢出。
- 缓存刷新：全部静态资源 `?v=` 升级到 `3.2.22`。

## V3.2.21 更新（2026-08-19）· 出厂默认天气区（中国天气网天气条）

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.20 → 3.2.21）。

本版把「页首天气区」的**出厂默认代码**设为中国天气网天气条，升级后首页开箱即有天气显示：
- **出厂默认天气区**：`weatherCode` 默认值改为
  `<iframe width="500" height="40" src="https://i.tianqi.com/?c=code&a=getcode&id=40&icon=1" frameborder="0"></iframe>`
  （中国天气网 i.tianqi.com 天气条），首页页首直接显示；后台「外观 → 首页行为 → 页首天气区代码」仍可随时修改或清空（留空隐藏）。
- 三处默认值同步：后端 `defaultSite()`、前端 `DEFAULT_SITE`、后台 `normalizeAppearance()`（复原默认外观时也回到该默认天气条）。
- 仅 iframe 类嵌入代码直接生效；script 类请放「外观 → 页眉 HTML」。
- 版本号 3.2.20 → 3.2.21；缓存刷新：全部静态资源 `?v=` 升级到 `3.2.21`。

## V3.2.20 更新（2026-08-19）· 搜索引擎可设 / 首页开幕动画 / 天气区 / 免密登录

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.19 → 3.2.20）。

本版一次性落地六项首页与后台增强：
- **搜索引擎默认为百度，后台可设置**：后台「外观」→「首页行为」可选择默认搜索引擎（百度/必应/谷歌/360/综合 SearXNG），首页进入自动预选该引擎（仍可手动切换）；选「综合」需先在「设置」启用 SearXNG。
- **帮助说明·常见问题标题左对齐**：帮助视图内面板主标题、副标题与全部问答标题强制 `text-align:left`，消除任何可能的居中继承。
- **首页进入开幕动态**：新增启动遮罩（LOGO 脉冲 + 加载条滑动 → 淡出移除），后台「外观」可开关；系统开启「减少动态效果」时自动跳过，不打扰。
- **页首天气区，后台可设代码**：后台「外观」粘贴天气插件 iframe 嵌入代码（和风天气/心知天气等），首页页首即显示天气卡片；留空隐藏。
- **网址登录用户名 / 密码**：后台网址表格新增「用户名」「密码」两列（内联编辑、自动保存），数据随链接一起备份/还原。
- **可免密登录**：设置过账号密码的链接，前台卡片右下角出现钥匙按钮，打开免密登录中转页 `/login.html?id=<id>`——「免密直达」把账号密码拼进 URL（HTTP Basic 认证，适合路由器/NAS/监控等设备）自动登录，亦支持显示/复制账号密码、浏览器自动填充提示；前台链接接口不返回密码明文（仅返回「已设置」标记），凭据仅存本机数据卷。
- 版本号 3.2.19 → 3.2.20；缓存刷新：全部静态资源 `?v=` 升级到 `3.2.20`。

## V3.2.19 更新（2026-08-18）· 后台排版字体与对齐全优化

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.18 → 3.2.19）。

本版对后台管理界面做一次**统一的字体与对齐优化**，让信息层级更清晰、阅读更顺：
- **统一字号刻度**：引入一套 CSS 变量字号体系（`--fs-display` / `--fs-title` / `--fs-sub` / `--fs-section` / `--fs-body` / `--fs-sm` / `--fs-xs` / `--fs-tiny`），全站文字、标题、标签、说明、徽标、表头、输入控件统一引用，消除此前散落的 px/rem 混用，后续改字号只动一处。
- **主标题 / 副标题明确区分**：面板主标题（`.nav-panel-head h3`、弹窗标题）放大到 1.15rem、800 字重；副标题（面板说明文字）降为 0.9rem、500 字重并改用浅色，二者在字号、字重、颜色上形成清晰的主次层级。
- **列宽标准化**：链接表格列宽改为变量驱动并统一为规整刻度——选择列 44px、分类列 120px、标记列 90px、操作列 120px（名称/地址列自适应），表头与单元格共用同一套宽度，列宽更稳定一致。
- **全左对齐**：将表格头「全选 + 计数」栏、批量操作栏、空状态文案、图标自检结果文本等由两端对齐/居中改为统一左对齐；后台所有标题与正文保持左对齐（登录页作为独立鉴权页仍居中，属常规设计）。
- **缓存刷新**：全部静态资源 `?v=` 升级到 `3.2.19`。

## V3.2.18 更新（2026-08-18）· 后台布局重构 + 网址自动保存/查重

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.17 → 3.2.18）。

本版针对「右下动态区压到右上固定头部、二者重叠」的反馈，放弃之前脆弱的 sticky 方案，改为**结构化解法**：
- **布局重构**：右侧主内容改为 flex 纵向布局——右上固定头部（统计概览 + 全局工具条）设为 `flex:0 0 auto` 常驻不动；右下动态内容全部收纳进新增的 `.content-scroll` 独立滚动容器。动态内容只在容器内部滚动，**永远走不到固定头部之下**，从结构上彻底消除重叠，不再依赖 sticky 与浏览器/ webview 兼容性。
- **左栏 + 右上栏固定、右下栏动态**：左栏（网址分类 + 导航设置）与右上头部各自在固定高度内独立滚动；右下内容区为唯一滚动区。
- **网址输入即自动保存**：在链接表格点「新增网址」或「编辑」进入内联编辑行后，名称与网址填写完整且合法，停输 1.5 秒或焦点移出该行即**静默自动保存**（无需点「保存」按钮，toast 提示「已自动保存」）；点「取消/保存」等同行按钮不触发自动保存。
- **网址查重提示**：在网址输入框输入/修改时实时比对全站已有网址（归一化 host+path 去重），若命中已在别处存在的网址，立即在该行下方红字提示「⚠ 该网址已存在（分类 / 名称），请确认是否重复」；查重命中时不自动保存，仅提示，避免产生重复项。
- **帮助说明·常见问题**：保持全左对齐。
- **缓存刷新**：全部静态资源 `?v=` 升级到 `3.2.18`。

## V3.2.17 更新（2026-08-18）· 进一步修复后台固定窗/动态窗「重叠」

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.16 → 3.2.17）。

本版核心：在 V3.2.16 的基础上，用真实 Chromium 渲染逐宽度复核，确认代码层面已无侧边栏与动态窗内容重叠；但仍收到部分环境（尤其是飞牛 webview / 浏览器缓存旧 CSS）的反馈，故做进一步结构加固与缓存刷新。
- **结构加固**：给 `.content`（动态窗主容器）增加 `position: relative; z-index: 1`，给 `.admin-view`（四个导航设置视图）增加 `position: relative`，让右侧内容列形成独立的层叠上下文，彻底隔离侧边栏与视图，避免在个别浏览器/旧 webview 中出现渲染错位。
- **帮助说明·常见问题**：进一步强化左对齐（`.help-faq` / `.help-q` / `.help-a` 显式 `text-align: left`），并确保 `details` 内容默认继承左对齐。
- **缓存刷新**：所有静态资源 `?v=` 参数同步升级到 `3.2.17`，配合飞牛重装强制浏览器拉取最新 `admin.css`，根治「已升级但仍见旧版重叠」的现象。
- 涉及文件：`admin.css`（`.content` / `.admin-view` 定位上下文、`.help-faq` 左对齐）。
- 八处版本常量 + manifest + RELEASE_NOTES 同步升到 3.2.17。

## V3.2.16 更新（2026-08-18）· 修复后台固定窗/动态窗「重叠」

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.15 → 3.2.16）。

本版核心：修复 V3.2.15 后台重排后出现的视觉重叠问题。
- **根因一**：动态背景画布 `#bg-particles` 为 `position: fixed; z-index: 1`，而后台内容区（侧边栏 + 动态窗）此前没有设置层级，导致粒子连线被绘制在侧边栏菜单与内容之上，看起来像「主标签页导航 / 底部快捷访问 / 自建页面 / 帮助说明」等条目与背景重叠。修复：给 `.dash-grid` 增加 `position: relative; z-index: 2`，把整个后台内容抬到画布之上。
- **根因二**：动态窗固定头部（统计概览 + 全局工具条）在 V3.2.15 改为常驻 sticky，但容器本身透明，下方视图内容滚动上来时会从头部缝隙/边缘透出，像「重叠」。修复：给 `.dynamic-header` 增加不透明背景 `var(--adm-bg)` + 底部阴影，与内容区明确分隔。
- **帮助说明·常见问题**：明确左对齐（`text-align: left`、占满面板宽度），与后台整体左对齐风格统一。
- 涉及文件：`admin.css`（`.dash-grid` 层级、`.dynamic-header` 不透明背景、`.help-faq` 左对齐）。
- 八处版本常量 + manifest + RELEASE_NOTES 同步升到 3.2.16。

## V3.2.15 更新（2026-08-18）· 后台「固定窗 + 动态窗」排版重排

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.14 → 3.2.15）。

本版核心：按附件 PDF 重排后台管理界面，明确划分为「固定窗」与「动态窗」。
- **固定窗**：左侧侧边栏保持固定，上半部分为「网址分类」（含过滤搜索与分类计数），下半部分为「导航设置」（主标签页导航 / 底部快捷访问 / 自建页面 / 帮助说明）。
- **动态窗**：右侧主区域顶部常驻「统计概览」四卡 + 「全局工具条」（改密码 / 设置 / 外观 / 刷新图标 / 图标自检 / 重置），切换左侧分类或导航设置项时，仅下方内容区随之变化。
- 网址列表的搜索、新增网址、导入 JSON、导入书签、导出操作，从全局工具条移入「链接视图」内容区顶部独立显示，与导航设置类视图互不干扰。
- 统计概览「已标记」卡片图标由调色板 `fa-palette` 改为星标 `fa-star`，与附件设计稿保持一致。
- 涉及文件：`admin.html`（结构重排为 `dynamic-header` + `links-actions`）、`admin.css`（新增动态窗固定头部与链接操作栏样式）、`admin.js`（统计卡片图标）。
- 其余后台功能（内联编辑、拖拽排序、批量操作、数据备份/还原、动态背景、SearXNG 设置、重置二次确认等）均不变。
- 八处版本常量 + manifest + RELEASE_NOTES 同步升到 3.2.15。

## V3.2.14 更新（2026-08-18）· 后台「重置」按钮二次点击确认

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.13 → 3.2.14）。

本版核心：防误触——把后台工具栏「重置」按钮从原生 confirm 弹窗改为按钮内二次点击确认。
- 第一次点击：按钮立即变红并带脉冲动画，文字变为「⚠️ 再次点击确认清空/重置」，3 秒内再点才执行。
- 3 秒内无第二次点击：按钮自动恢复为「重置」常态，不会误清空。
- 第二次点击（红色态）：真正调用 `/api/reset` 恢复默认数据，请求完成后按钮恢复常态。
- 相关改动：`admin.html` 按钮结构预留文本容器；`admin.js` 重写点击逻辑（ armed 态 + 3 秒定时器 + 恢复函数）；`admin.css` 新增 `.btn.reset-armed` 红色态与 `reset-pulse` 动画。
- 其余后台布局、功能与数据接口不变。
- 八处版本常量 + manifest + RELEASE_NOTES 同步升到 3.2.14。

## V3.2.13 更新（2026-08-16）· 后台删除冗余「链接管理」菜单项

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.12 → 3.2.13）。

本版核心：去重后台「导航设置」左侧菜单。
- 删除与「上网址分类」重复的「链接管理」菜单项（点击它进入的右侧视图本身即「网址分类网址列表」，与左侧网址分类列表功能重叠）。
- 「网址分类网址列表」作为后台默认主视图保留（统计概览、改密码、工具栏、分类表格不变），并仍由左侧「网址分类」列表进入/筛选、切回。
- 左侧「导航设置」现有四项：主标签页导航 / 底部快捷访问 / 自建页面 / 帮助说明。
- 其余布局与功能（动态背景、自建页面、帮助说明、数据备份等）不变。
- 八处版本常量 + manifest + RELEASE_NOTES 同步升到 3.2.13。

## V3.2.12 更新（2026-08-16）· 修复飞牛桌面图标不显示

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.11 → 3.2.12）。

本版核心：修复飞牛安装后桌面/应用中心图标空白的问题。
- 根因：`app/ui/config` 中 `"icon": "images/icon_{0}.png"` 误把 `{0}` 当成主题占位符，实际飞牛会将其解析为**尺寸占位符**（64 / 256），于是系统去找 `icon_64.png` / `icon_256.png`，文件不存在导致图标空白。
- 修复：
  - 将 `app/ui/config` 的 `icon` 改为 `"images/icon-{0}.png"`（连字符尺寸占位符）。
  - 在 `app/ui/images/` 下补齐 `icon-64.png`（64×64，来自 ICON.PNG）和 `icon-256.png`（256×256，来自 ICON_256.PNG）。
  - 删除错误的 `icon_0.png` / `icon_1.png` 主题命名图标。
  - 顺手把 `protocol` 从空字符串修正为 `http`，避免部分场景下协议降级或端口不可达。
- 外层 `ICON.PNG` / `ICON_256.PNG` 保持不变（应用中心/安装流程使用）。
- 八处版本常量 + manifest + RELEASE_NOTES 同步升到 3.2.12。

## V3.2.11 更新（2026-08-16）· 后台「导航设置」新增主标签页/快捷访问入口

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.10 → 3.2.11）。

本版核心：按用户要求，把后台「主标签页导航」「底部快捷访问」两个列表从右下的「三大列表子标签行」移入左侧「导航设置」侧边栏，作为独立入口；原子标签行移除，右下链接区仅保留「网址分类网址列表」。
- 左侧「导航设置」顺序现为：链接管理 / 主标签页导航 / 底部快捷访问 / 自建页面 / 帮助说明。
- 两个列表各自成为独立顶层视图（nav-tabs-view / nav-quick-view），点击左侧入口即切换，编辑/保存逻辑不变。
- 其余后台布局与功能（右上统计与改密码、工具栏、自建页面、帮助说明、动态背景等）均不变。
- 八处版本常量 + manifest + RELEASE_NOTES 同步升到 3.2.11。

## V3.2.10 更新（2026-08-16）· 后台可选动态背景（粒子/光晕/极光）

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.09 → 3.2.10）。

本版核心：把动态背景从「前台写死粒子」升级为后台「外观」里**可选**的效果。
- 后台「外观」新增「动态背景」设置：**效果**（粒子连线网络 / 鼠标光晕跟随 / 极光渐变流动 / 关闭）+ **应用页面**（仅前台 / 仅后台 / 前后台都启用），保存后前台刷新即时生效。
- 底层用统一引擎 `js/background.js` 替换原 `js/particles.js`，三种效果 + 关闭共享同一套 canvas 与主题适配、高分屏、减少动态效果降级。
- 八处版本常量 + manifest + docker-compose 同步升到 3.2.10。

## V3.2.09 更新（2026-08-16）· 前台首页鼠标动态粒子连线背景

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.08 → 3.2.09）。

本版核心：前台首页新增「鼠标动态粒子连线」背景，模拟 DeepSeek 风格的科技感交互。
- 纯原生 canvas 实现，零依赖；`<canvas id="bg-particles">` 固定浮层，`pointer-events:none` 不拦截任何点击。
- 粒子间自动连线（距离越近越亮），鼠标附近粒子向光标连线并高亮；自适应明暗主题配色与高分屏(DPR)。
- 尊重 `prefers-reduced-motion`：开启「减少动态效果」时降级为静态单帧。
- 八处版本常量 + manifest + docker-compose 同步升到 3.2.09；新增 `js/particles.js` 并在 index.html 引用 `?v=3.2.09`。

## V3.2.08 更新（2026-08-16）· 刷新三个出厂内置工具页

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.07 → 3.2.08）。

本版核心：采用用户本地编辑后的最新版本，刷新三个随镜像发布的离线工具页（首次启动自动播种为「自建页面」并挂快捷访问）：
- 网络测速 `/pages/TEST.html`（真实 RTT 延迟测量）
- 智能税务计算器（一般纳税人）`/pages/customs.html`
- 智能理财计算器 `/pages/zlcalculator.html`（内联 canvas 图表，无外部 CDN）
- 八处版本常量 + manifest + docker-compose 同步升到 3.2.08；三个页面的快捷访问入口（/pages/ 形式）维持不变。

## V3.2.07 更新（2026-08-16）· 后台重新布局 + 标题左对齐

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.06 → 3.2.07）。

本版核心：按用户要求重排后台管理界面，并统一标题左对齐。
- 后台整体维持「左 / 右上 / 右下」结构：左 = 导航设置（侧边栏）、右上 = 链接总数统计卡 + 改密码框（固定常显，不随下方子列表切换隐藏）、右下 = 链接表格。
- 右下链接表格用**子标签**整合三大列表，无需在顶层视图间来回切换：
  - 【网址分类网址列表】（原链接管理表格，含搜索 / 新增 / 导入 / 导出 / 批量移动·删除）
  - 【主标签页导航列表】（原主标签页导航编辑器）
  - 【底部快捷访问列表】（原底部快捷访问编辑器）
- 左侧导航设置精简为：链接管理 / 自建页面 / 帮助说明；主标签页与底部快捷访问改为右下子标签。
- 帮助说明 · 常见问题入口保留（左侧导航设置 → 帮助说明），后台标题统一**左对齐**（帮助区去掉了原来的居中容器）。
- 八处版本常量 + manifest + docker-compose 同步升到 3.2.07。

## V3.2.06 更新（2026-08-16）· 修正首页分页位置 + 锁定左/右上/右下布局

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.01 → 3.2.02 → 3.2.03 → 3.2.04 → 3.2.05 → 3.2.06）。

本版核心：按用户要求锁定页面布局结构。
- 修正前台首页分页「第 1 页 / 共 1 页」位置：此前删除"添加网址"按钮后，`.links-footer` 桌面端 `justify-content: space-between` 在仅剩一个子元素时把分页推到了**左边**；改为 `flex-end`，分页控件稳定靠**右**（右下角）。移动端仍居中。
- 后台导航设置保持「左 / 右上 / 右下」结构不变：左 = 侧边栏（网址分类 + 导航设置），右上 = 统计卡片（链接总数等）+ 改密码按钮，右下 = 链接表格。两项框【链接总数】【改密码】原样保留。
- 前台与后台均保持上述布局锚点，未做破坏性改动。

## V3.2.05 更新（2026-08-16）· 全站默认 deepseek 风格深色主题

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.01 → 3.2.02 → 3.2.03 → 3.2.04 → 3.2.05）。

本版核心：应要求把首页+后台整站背景换成 deepseek harness 风格的深色主题。deepseek harness 真实配色为页面底色近黑 `#0a0a0a`（叠加层深海军蓝 `#0d1017`）+ 品牌蓝 `#6799fe` + 白色文字。具体改动：
- 前台 `.dark-mode` 补上 body 背景（修复原"切深色只变字不变底"的缺陷），采用 `linear-gradient(135deg, #0a0a0a, #0d1017)`；强调色 `--primary-color` 改为 deepseek 品牌蓝 `#6799fe`。
- 前台 `darkMode` 默认值翻为 `true`，且"读取已存主题"逻辑改为默认深色（仅当用户显式选过浅色才浅色）。
- 后台 `admin.js` 同样默认深色（初值 + `start()` 读取逻辑），后台 `body.dark-mode` 底色调为 `#0f1318` 与 deepseek 近黑一致。
- 仍保留亮/暗切换按钮，用户可随时切回浅色。版本号 3.2.04→3.2.05（八处常量 + manifest + compose 同步）。

## V3.2.04 更新（2026-08-16）· 资源缓存破版本（根治"重装后仍见旧按钮"）

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.01 → 3.2.02 → 3.2.03 → 3.2.04）。

本版核心：彻底解决"我明明重装了新包、首页却还是旧的 +添加网址 按钮"的问题。经逐行排查，源码里首页「+添加网址」入口（底部 add-link-btn-bottom、右下角 floating-add）已于 3.2.03 移除，唯一残留是默认隐藏的添加弹窗标题（不会在首页显示）。用户侧反复看到，实为浏览器按 URL 缓存了旧的 index.html / script.js。本次给 index.html 与 admin.html 的全部本地 css/js 引用追加 `?v=3.2.04` 版本号参数：版本号一变 URL 即变，浏览器必然重新拉取，杜绝"改了却看不到"。版本号从 3.2.03 升到 3.2.04（八处版本常量 + manifest + compose 同步）。「第 1 页 / 共 1 页」分页保持右下角；后台「导航设置」右上角「链接总数」「改密码」两个框保持保留。

## V3.2.03 更新（2026-08-16）· 版本号递增 + 删除「+添加网址」入口

按版本号约定 A.B.CC，每个内部测试构建 CC +1（3.2.01 → 3.2.02 → 3.2.03）。本版相对 3.2.01 修正：彻底删除首页「+添加网址」入口（移除底部 add-link-btn-bottom 按钮、右下角 floating-add 浮动按钮及全部 JS 引用，拆包核验内层无残留）；并将版本号从 3.2.01 升到 3.2.03（此前两次修复漏加版本号，本次补齐）。

## V3.2.01 更新（2026-08-16）· 版本号约定落地（A.B.CC）+ 页脚/后台版本锁定

按广哥版本号约定 `A.B.CC`（A=大版本 / B=小版本 / CC=两位数测试版、从 01 起）首次落地，本版为 `3.2.01`，后续每个内部测试构建 CC +1（`3.2.02`、`3.2.03`、`3.2.04` …）便于区分不同测试版。页脚与后台版本号统一由构建版本（`server.js VERSION` / `script.js CONFIG.VERSION` / `admin.js APP_VERSION` 同源）动态注入，去除硬编码，永远与「更新」同步。

（root 镜像树与 fpk 构建树功能一致；出厂默认自建页面模板、后台「帮助说明」、登录「忘记密码」等详见发布 Release。）

## V3.1.6 更新（2026-08-16）· 数据保护（升级/迁移不丢数据）

**核心目标：更新、迁移、重装都不要丢用户数据。**

- **后台一键「下载备份 / 还原」**：后台「自建页面」视图底部新增「数据备份 / 还原」面板。
  - 「下载备份」：把整个数据卷（`/app/data`，含 `config.json`、`links.json`、`pages/`、`site/` LOGO、`favicons/`、`secret.txt`）打包成 zip 下载，零依赖实现（内置 zip 读写，无需外部库）。
  - 「还原备份」：上传 zip 即还原；**还原前系统先自动备份当前数据到 `backups/pre-restore-<时间戳>.zip`，可随时回滚**；有二次确认防止误覆盖。
- **飞牛升级前自动备份**：`cmd/upgrade_init` 在飞牛升级时自动把数据卷打包到 `shares/qiyi-nav/backups/`，保留最近 7 份，升级异常可回滚。
- **飞牛卸载前自动备份**：`cmd/uninstall_init` 在卸载时额外把数据复制到飞牛用户可见卷（`/vol1/1000` 等），防止卸载清理应用目录导致数据丢失。
- **Docker / GHCR 版**：新增 `scripts/backup.sh`（默认备份 `./data` 到 `./backups`，保留最近 7 份），升级前 `bash scripts/backup.sh` 即可；或直接用后台「下载备份」。
- **数据持久化机制（已经过验证）**：所有用户数据均落在 `DATA_DIR`（默认 `/app/data`），镜像/容器重建不丢；种子数据放在挂载点之外的 `/app/default-seed.json`，挂载空卷也能初始化默认链接。
- 全站版本号统一为 3.1.6。

## V3.1.5 更新（2026-08-16）· 后台"有更新版"提示

**后台版本更新检查**
- 新增公开接口 `GET /api/update-check`：返回 `{enabled, current, latest, available, url}`，仅含版本信息、无敏感数据。
- 后台右上角新增「检查更新」按钮，进入后台时也会静默检查一次；发现新版本时顶部弹出横幅，显示"发现新版本 Vx.y.z（当前 V当前）"并附「前往更新」链接。
- 更新源可配置（环境变量，二选一）：
  - `UPDATE_REPO=owner/repo` → 比对 GitHub Releases 最新版（推荐）；
  - `UPDATE_CHECK_URL=https://...` → 自定义端点，返回纯文本版本号或 JSON `{version,url}`。
- 未配置时后台仅显示当前版本、不弹提示（零副作用）；网络不可达/被墙时静默降级，不报错不打扰。
- 服务端带 1 小时结果缓存，避免频繁请求 GitHub（未认证限流 60 次/小时/IP，个人部署足够）。

**注意**：要让更新提示生效，需在部署环境（Docker/FNOS 的 `.env` 或 `docker-compose.yml` 的 `environment`）设置 `UPDATE_REPO`（你的仓库名）。例如 `UPDATE_REPO=yourname/hao123`。

## V3.1.4 更新（2026-08-15）· 自建页面（上传/编辑 HTML + 一键加入快捷访问）

**后台「自建页面」管理（左侧导航新增「自建页面」入口）**
- 新增私有接口（均鉴权）：`GET /api/pages`（列表）、`POST /api/pages`（创建/上传，同名 409）、`GET /api/pages/<name>`（读取）、`PUT /api/pages/<name>`（保存编辑）、`DELETE /api/pages/<name>`（删除）；正文上限 2MB。
- 页面文件安全落盘到数据卷 `DATA_DIR/pages/`（容器重建不丢），文件名经 `safePageName` 严格白名单校验（中文/字母/数字/下划线/连字符 + `.html`/`.htm`，≤60 字），杜绝目录穿越与覆盖系统文件。
- 前台公开路由 `/pages/<name>` 直接访问（如 `/pages/我的笔记.html`），经静态校验后读取并 `no-cache` 返回，绕开原 `index.html/admin.html/css/js/assets` 的严格静态白名单，满足"上传到根目录般直接访问"的诉求又保持安全。

**编辑器（上传 + 在线修改）**
- 「新建空白页」给出现成 HTML 模板；「上传 HTML 文件」直接选本地 `.html` 读入编辑器预览。
- 编辑器左写源码、右沙箱 iframe 实时预览（`sandbox="allow-scripts allow-same-origin"`），保存即生效；文件名编辑时禁用（避免改名歧义）。

**一键加入前台「快捷访问」**
- 每张页面卡片的闪电图标：读取 `site.quickAccess`（经 `PUT /api/settings` 整体合并，安全裁剪），追加 `{text, href:'/pages/<name>', icon:'fas fa-file-code', target:'_blank'}`，按 `href` 去重，前台底部快捷访问即时可用。

## V3.1.3 更新（2026-08-15）· 图标自检 + 缺图占位

**图标自检（后台「图标自检」按钮）**
- 后端新增 `GET /api/icon-check`：扫描全部链接的域名，统计 favicon 获取情况，返回域名总数、已获得数、未获得数与成功率，并列出未获得图标的域名（含命中链接数、样例名称、是否仍可在线获取）。支持 `?deep=1` 深度检测（并发在线探测未缓存域名的实际可获取性，并发上限 8）。
- 后台「图标自检」弹层：四宫格概览（总数 / 已获得 / 未获得 / 成功率）+ 可勾选「深度检测」+ 域名清单（未获得的排在最前，可一键「重抓未获得图标」触发后台重新抓取）。

**缺图占位图标（前台）**
- 链接 favicon 加载失败（`/api/favicon` 返回 404 或自定义图标地址失效）时，原来直接隐藏空白，现改为显示内联 SVG 齿轮占位图标（代表"自检 / 自动获取"），不再空白 / 裂图；仅替换一次（`dataset.fb` 防重复）。
- 同时修复链接 `href` 未转义的问题（改用 `escapeHtml` 输出，避免地址含引号破坏标签）。

## V3.1.1 更新（2026-08-15）· 主页 LOGO 移至日期左侧 + 上传限尺寸压缩
- 主页（前台）LOGO 从"页头上方独占一行"改为与两个日期（公历时间 / 农历日期）同排、位于其左侧，用 `.header-main > .header-brand` 弹性分组，不再占整行。
- 后台「LOGO 设计」上传增加前端 canvas 限尺寸压缩：上传图最大边限制为 512px，含透明通道导出 PNG、否则导出 JPEG(0.9)，防止自定义 LOGO 尺寸过大/错乱、文件过大。
- 桌面图标维持合规 64×64 / 256×256（飞牛不刷图标缓存，须"应用中心全卸载→重装"才生效）。

## V3.1.0 更新（2026-08-15）· 前后台 LOGO 自定义 + 搜索快捷项可编辑 + 桌面图标规格修正

**前后台 LOGO 后台可上传自定义（核心诉求②）**
- 后台「外观」弹层新增「LOGO 设计（前后台通用）」区：前台 LOGO（页头 + favicon）与后台 LOGO（登录窗 + 应用栏）各自独立上传/恢复默认。
- 上传走零依赖方案：前端转 dataURL(base64) → `POST /api/site/upload-logo` → 服务端落盘 `data/site/<which>-logo.<ext>`（≤2MB，限 png/jpg/gif/webp/svg）。
- 未上传时回退出厂卡通图 `assets/logo-default.png`；前端按 `hasCustom` 自动切换，并同步刷新 favicon。
- 服务端新增 `/api/site/logo/<frontend|backend>` 读取接口与 `/api/site/clear-logo` 恢复接口；`/api/settings` 的 PUT 路径禁止经它改 logo（只能走专用上传接口），避免被普通设置覆盖。
- 静态白名单新增 `/assets/`，修复旧版 `/img/logo.jpg` 返回 403 导致 LOGO 裂图的问题。

**搜索快捷项后台可增删改（核心诉求④）**
- 搜索框下方 6 项（天气预报/今日新闻/股票行情/汇率换算/快递查询/地图导航）从写死改为 `site.quickSearches` 可配置。
- 后台「外观」弹层新增「搜索快捷项」编辑器：可增/删/上下移、改文字与跳转 URL；支持 `{q}` 占位，点击直接 `window.open` 打开真实页面（而非仅塞进搜索框）。
- 服务端对 `quickSearches` 做防御性裁剪（每项 text≤20/url≤500、最多 30 项、过滤空 text），PUT 时不会被恶意超长数据撑爆。

**桌面图标规格修正（核心诉求②）**
- `fnos/qiyi-nav/ICON.PNG`(64)、`ICON_256.PNG`(256)、`app/ui/images/icon_64.png`、`icon_256.png` 全部按飞牛规范重制为合规 64×64 / 256×256（旧版外层 ICON 是 640×640，不符规范可能被飞牛弃用回退旧图）。
- ⚠️ 飞牛不会刷新桌面图标缓存：**必须先在飞牛「应用中心」全卸载旧版 → 再重装此包**，才能把桌面图标真正换过来；单纯「更新容器」无效。

**保留能力**：页脚整段 HTML 可改、图标选择器居中模态、约230个图标全量网格、表格行内联编辑、自动获取网站 ICO、分时段问候、书签导入、拖拽排序、通知窗顶部居中紧凑小卡片、全局自定义头像图等 V3.0.x 全部能力均保留。

## V3.0.15 更新（2026-08-15）· 通知窗顶部居中紧凑 + 全局 logo 替换为自定义头像图

**通知窗（保存提示/操作提示）参考登录窗改为顶部居中紧凑小卡片**
- 之前底部居中 + 横排 `display:flex` + 较宽 max-width，文案稍长就视觉上"扁而长"。
- 现在改为**顶部居中（top:22px; translate -50%,-16px → 0）**，更小字号（.82rem）、更窄 padding、inline-flex 自动收缩，单行短文案整条只有几像素高，长文案也不会横向撑满。视觉效果与登录窗"小卡片"风格统一。
- 飞牛 webview 老内核不支持 CSS min()/max()/clamp()，仍只用基本数值 + 窄屏媒体查询兜底。

**全局 logo 替换为自定义头像图（PNG）**
- 登录窗 logo（原来 `fa-link` 渐变方块）→ `/img/logo.jpg`（圆形 64×64 圆角容器内显示）
- 后台顶栏 logo（原来 `fa-link` 渐变方块）→ `/img/logo.jpg`（38×38）
- 前台 favicon（原来 SVG 链接图标）→ `/img/logo.jpg`（浏览器标签页 / 收藏夹显示）
- 飞牛 FPK 应用图标 `ICON.PNG / ICON_256.PNG / app/ui/images/icon_{64,256}.png` 全部替换为新图（飞牛桌面/应用库/任务栏显示）

---

---

## V3.0.14 更新（2026-08-15）· 页脚改为整段 HTML 可改 + 发布者/开发者署名

- **页脚重构为「整段 HTML 代码可改」**：删除后台外观里的「页脚功能链接」结构化编辑器（图标/名称/网址逐行编辑那套）；改为由「整页页脚 HTML」文本框**接管整个页脚**——在此直接编写/修改整段页脚代码（系统信息、版权、链接等全部可改），留空则用出厂默认页脚。默认值已内置为完整页脚标记，打开即见「整个页脚代码」，保存后前台整段替换生效。前台 `applySiteConfig` 在 `footerHtml` 非空时整段替换 `<footer>` 并复跑年份/访问量动态填充；为空时退回原默认页脚 + 功能链接渲染。
- **根治「保存提示窗还是很长」**：根因是飞牛 webview 内核较老、**不支持 CSS `min()` 函数**，导致 `.notification` 的 `max-width: min(92vw,420px)` 整条声明被当作非法丢弃、宽度限制失效。V3.0.14 改为 `max-width: 420px`（通用写法）+ 窄屏 `@media (max-width:480px)` 单独放宽到 `92vw`，彻底稳定生效。
- **manifest 署名**：发布者 `distributor = Martin_Feng`，开发者 `maintainer = 帝赞科技`。
- 版本升 3.0.14；manifest / 文档同步更新。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.14`（或 `:latest`）。数据卷不变，可从 3.0.13 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.13 更新（2026-08-15）· 图标选中真正生效 + 提示窗不再撑满 + 编辑器单行化

- **根治「图标选了没应用上」**：之前图标网格的点击事件挂在 `document` 委托上，在飞牛 webview 偶发不触发；且选中后只改按钮 `<i>`、没有即时反馈，容易误以为没选上。V3.0.13 改为**网格自身委托**（`#icon-pick-grid` 直接监听 `.icon-opt`，不受外层上下文影响），选中后**立即重渲染对应编辑器整行**（图标变化一目了然）+ 弹提示「图标已更新（点对应『保存』后生效）」，点选 100% 落盘到 `appearanceState`，再点编辑区「保存」即持久化。
- **根治「保存提示窗也很长」**：通知条（toast）原本无宽度上限，长文案会横向撑满整屏。V3.0.13 给 `.notification` 加 `max-width: min(92vw, 420px)` + 自动换行，提示窗变为紧凑小条，不再拉成横长条。
- **编辑器改为单行布局（图标在最前）**：主标签页导航 / 底部快捷访问 / 页脚功能链接三个编辑器，从「多行竖排」改为**一行**：`[图标] [名称] [网址] [上移/下移] [删除] [显示该标签]`，图标选择器移到行首，窄屏自动换行，信息密度更高、操作更顺手。
- 版本升 3.0.13；manifest desc 与文档同步更新。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.13`（或 `:latest`）。数据卷不变，可从 3.0.12 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.12 更新（2026-08-15）· 根治「未找到该图标所属条目」+ 弹窗过长

- **根治图标选择器报错「未找到该图标所属条目」**：之前靠 `closest()` 在 DOM 里层层找 `.editor-row` 和容器 ID（`#nav-tabs-editor` 等）来定位要写回的条目，任何层级/重渲染抖动都会返回 null 而报错。V3.0.12 改为**按钮自身携带 `data-idx` + `data-target`**（tabs/quick/footer），`resolveIconTarget` 直接读这两个属性映射到对应数组与下标，**彻底摆脱脆弱的 DOM 遍历**，点哪个图标就写哪个，不再报错。
- **根治「上下窗口很长、几个确认窗口都很长」**：根因是 `.modal` 用 flex 居中（`align-items:center`）时，内容高于视口会把顶部顶出可视区且无法滚动。V3.0.12 给 `.modal` 加 `overflow-y:auto`、`.modal-content` 改用 `max-height: calc(100vh - 40px)` + `margin:auto`——内容超高时**弹窗本体可滚动、顶部不再被裁、始终居中**（经典 flex 居中溢出修复），图标模态与后台其它确认窗（改密码 / 集成 / 外观）一并受益。
- 图标模态保持「外层模态 90vh 滚动 + 内层网格 56vh 滚动」双保险，约 230 个图标全量可选 + 搜索。
- 版本升 3.0.12；manifest desc 与文档同步更新。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.12`（或 `:latest`）。数据卷不变，可从 3.0.11 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.11 更新（2026-08-15）· 图标选择器改为居中模态（根治飘远）

- **彻底换架构**：连续三次微调 `position:fixed` 坐标（贴边 / 视口收敛 / 基准补偿）在飞牛 webview 下均不稳定，弹层仍飘远、点不到、选不上。V3.0.11 直接放弃坐标定位思路——图标选择器从「侧栏内贴边弹层」重构为**居中模态弹窗**（复用 `#pw-modal`/`#settings-modal`/`#appearance-modal` 同一套 `.modal { inset:0 + flex 居中 }`），**不依赖任何 top/left 计算**，从根本上不可能飘远。
- 点击主标签页导航 / 底部快捷访问 / 页脚功能链接里的小图标按钮 → 弹出**屏幕中央**的图标选择窗，列出约 230 个图标全量网格 + 搜索框，点选即写回并自动关闭；按钮图标即时变化。
- 满足「所有弹窗居中」诉求：后台所有弹窗（改密码 / 集成设置 / 外观设置 / 图标选择）统一 flex 居中，风格一致。
- 分类网址仍走「自动获取网站 ICO」，不在此选择器内（保持你定的逻辑）。
- 版本升 3.0.11；manifest desc 与文档同步更新。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.11`（或 `:latest`）。数据卷不变，可从 3.0.10 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.10 更新（2026-08-15）· 图标弹层基准补偿，彻底修「飘远」

- **根因（关键）**：页面自身 CSS 没有持久 transform，但飞牛外壳窗口对 webview 内容施加了 transform，使 `position:fixed` 的定位参照从视口变成了飞牛外壳。后台 `#pw-modal`/`#settings-modal`/`#appearance-modal` 用 `inset:0`（铺满容器）+ flex 居中，所以即使有 transform 偏移也看不出异常（弹窗看着居中正常）；而图标弹层用 `top/left` 绝对坐标，transform 外壳的偏移直接暴露 → 弹层被甩到远离按钮处（"移位太远"）。V3.0.8/3.0.9 直接用按钮 `getBoundingClientRect`（视口坐标）设 fixed 的 top/left，恰好踩中这个坑。
- **修复（基准补偿法）**：打开图标弹层时，先把它 `appendChild` 到 `body` 并 `position:fixed`、暂设 `top/left=0`，**实测它在视口中的真实位置 `base`**（即飞牛外壳 transform 的偏移量）；再按按钮 `rect` 算出「贴着小图标边缘」的目标视口坐标 `(wantTop, wantLeft)`，最后 `top = wantTop - base.top`、`left = wantLeft - base.left` 反推回去。无论有无 transform 外壳，弹层都精确贴边；并保留视口 clamp（右侧溢出左移 / 下方放不下翻上方）。
- **图标的"没应用上"**：是飘远的连带症状——弹层飞到远处，图标选项点不到，自然像没生效。修好贴边后即可正常点选；选完图标需点对应编辑区的「保存」才会持久化（选的时候按钮图标即时变化）。
- 版本升 3.0.10；manifest desc 同步更新。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.10`（或 `:latest`）。数据卷不变，可从 3.0.9 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.9 更新（2026-08-15）· 图标弹层贴边 + 视口收敛（细节修复）

- **根因**：V3.0.8 把图标弹层 portal 到 `body` + `fixed`，但定位只用按钮 `rect` 未做边界收敛，且在弹层**尚未显示、尺寸为 0 时**就设了位置，加上后台模态 `backdrop-filter` 的 containing-block 干扰，最终弹层被甩到远离按钮的位置（"移位太远"）。
- **修复**：点击图标按钮时，**先显示并挂到 body，再实测弹层尺寸**，然后「贴着小图标边缘」定位（默认显示在按钮正下方、左对齐按钮左边）；并对整个弹层做 **视口 clamp**——右侧溢出则左移、下方放不下则翻到按钮上方，确保弹层永远贴着小图标且完整可见，绝不飘远。关闭弹层时复位内联定位样式，避免下次显示残留。
- **弹窗居中**：后台 `#pw-modal` / `#settings-modal` / `#appearance-modal` 等模态弹窗本身就是 flex 居中布局（保持不动）；图标选择器作为"小弹窗"按你的要求改为贴着小图标边缘（不做居中，避免遮挡按钮）。
- 版本升 3.0.9；manifest desc 同步更新。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.9`（或 `:latest`）。数据卷不变，可从 3.0.8 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.8 更新（2026-08-15）· 修复图标选择器「只显示一个图标」

- **根因**：主标签页导航 / 底部快捷访问的图标选择器弹层是 `position:absolute` 嵌在左侧 `.side-panel`（`overflow-y:auto` 滚动容器）里，绝对定位后代会被 overflow 容器裁掉——弹层根本弹不出来，用户只看到按钮上那一个当前图标，以为"只有一个、不能选"。
- **修复**：点击图标按钮时，把弹层 **portal 到 `document.body` + `position:fixed`**（按按钮位置定位），彻底逃离侧栏滚动裁剪，也兼容可能存在的模态 `backdrop-filter` 包含块；同时在弹层上挂 `_arr/_idx/_owner`，portal 后仍能正确写回对应条目图标。现在主标签页 / 底部快捷 / 页脚三处图标选择器都能**完整弹出约 230 个图标网格 + 搜索框**，随手点选。
- 版本升 3.0.8；manifest desc 同步更新。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.8`（或 `:latest`）。数据卷不变，可从 3.0.7 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.7 更新（2026-08-15）· 修正图标选择器落点 + manifest 文案

- **修正图标选择器落点（重要）**：`fa-wifi`/`fa-server`/`fa-tachometer-alt` 这类图标是在**主标签页导航 / 底部快捷访问**里由用户自己从网格挑选的（V3.0.6 已提供约 230 个全量图标 + 搜索框）。**分类网址的图标不走手动选择**，而是「自动获取网站 ICO」（编辑行里的「自动获取」按钮按网址补全名称/图标）。因此 V3.0.7 撤销了误加在「分类网址表格内联编辑行」里的图标选择器，恢复为名称/地址/分类/颜色四栏，分类网址图标回归自动获取。
- **保留并确认**：主标签页 / 底部快捷访问 / 页脚功能链接的图标选择器（约 230 个 + 搜索）正常工作，所有前台入口的图标都能「列出全部、随手点选」。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.7`（或 `:latest`）。数据卷不变，可从 3.0.6 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.6 更新（2026-08-15）· 浮标管理菜单 + 图标全量可搜 + 网址分类内联编辑

- **底部「管理面板」改为浮标功能菜单**：前台页脚的「管理面板」按钮取消，管理入口统一收进右下角浮标功能菜单（菜单内「网址管理」已置顶，需密码验证后跳转后台）。
- **图标选择器列出全部并可搜索**：主标签页 / 底部快捷访问 / 页脚功能链接的图标选择器，从约 50 个扩成全量常用 Font Awesome 实心图标（约 230 个），弹层内新增搜索框，按名称实时过滤，彻底告别手填 class。
- **网址分类「在原处直接改」，不再弹窗**：后台网址表格的「编辑」改为行内联编辑——名称 / 地址 / 分类 / 颜色直接在表格行里改，配「保存 / 取消 / 自动获取名称」；「新增网址」也在表格顶部直接出现内联编辑行。去掉了原来的抽屉弹窗，更直观。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.6`（或 `:latest`）。数据卷不变，可从 3.0.5 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.5 更新（2026-08-15）· 图标标准选择器 + 分时段问候 + 页眉页脚可恢复

- **图标改成标准选择器（不再手填）**：主标签页、底部快捷访问、页脚功能链接的图标，从「手填 `fas fa-wifi` 这类 class 名」改为点击弹出标准图标网格直接选，已选图标实时预览，杜绝填错。
- **问候语支持分时段多个问候**：问候语模式新增「分时段自动问候」——按当前时间（HH:MM）自动匹配多条时段文案，时段可增删、改起止时间与文案；原有的「自定义单一文案」模式保留。
- **页眉/页脚自定义代码（前后台通用）显示当前值 + 可恢复**：打开外观弹窗即显示当前已保存的页眉 HTML、页脚 HTML、全站 CSS（前后台通用）；每项旁有「恢复默认」按钮可单独清空，全部改完点「保存」生效。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.5`（或 `:latest`）。数据卷不变，可从 3.0.4 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.4 更新（2026-08-15）· 导航设置改为「左导分类、右修改窗口」

本版本把「主标签页导航」与「底部快捷访问」的管理交互，从「左框堆叠编辑器」升级为与「网址分类」完全一致的体验：

- **左侧新增「导航设置」分组**：后台左栏在「网址分类」下方新增「导航设置」，内含「主标签页导航」「底部快捷访问」两个可点击条目，与网址分类共用同一套选中高亮交互。
- **右侧三视图切换**：点击左侧条目，右侧主区在「网址表格 / 主标签页编辑器 / 快捷访问编辑器」之间切换——左导分类、右修改窗口，用法和网址分类一致。
- **编辑器移入右窗**：主标签页（顺序/名称/显隐）、快捷访问（增删/排序/改名/添加快捷）的编辑面板整体移到右侧，各自带独立「保存」按钮；外观弹窗仍负责问候语/页眉页脚代码/CSS/页脚功能链接/复原。
- **配置共享不变**：导航结构仍走 `config.json` 的 `site` 字段，`PUT /api/settings` 整体保存，与前台及外观弹窗共用同一份状态。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.4`（或 `:latest`）。数据卷不变，可从 3.0.3 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.3 更新（2026-08-15）· 导航结构编辑器迁入左框

本版本把「前台结构」管理从前台「外观」弹窗拆分到后台左框，与网址分类并列，操作更顺手：

- **左框新增「导航结构」管理区**：后台左侧栏在「网址分类」下方新增「主标签页导航（顺序/名称/显隐）」与「底部快捷访问」两个紧凑编辑器，增删、上下排序、改名、显隐、添加快捷均可直接在左框完成；底部「保存导航结构」一键保存。
- **外观弹窗精简为「风格」设置**：保留问候语（开关/自动时段/自定义文案）、页眉/页脚自定义 HTML 代码（前后台通用）、全站自定义 CSS、页脚功能链接、以及「复原默认外观」。打开即显示现有代码可改，避免误操作丢失。
- **共享外观状态**：左框与外观弹窗共用同一份 `site` 配置，任一处保存都整体落盘（`PUT /api/settings`），未保存的改动不会被弹窗打开覆盖。
- **左框可滚动**：窄屏下编辑器改为字段堆叠 + 按钮列，左框内部 `overflow-y` 滚动，不再撑爆视口。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.3`（或 `:latest`）。数据卷不变，可从 3.0.2 直接升级；**重装/更新需重建镜像**方能生效。

---

## V3.0.2 更新（2026-08-15）· 站点外观后台全自定义

本版本把「首页外观」交给后台管理，运营无需改代码即可定制：

- **底部「快捷访问」+ 页脚功能链接后台可编辑**：改为后台「外观」弹窗内的列表（增删、上下排序、改名称/地址/图标），不再写死在 HTML。
- **页眉/页脚「代码可修改模式」**：后台可填自定义 HTML 片段注入页眉、页脚，并支持「全站自定义 CSS」；前后台（首页 + 后台）同时生效。
- **问候语后台可设置**：可开关问候语横幅，可选「自动时段问候」或「自定义文案」。
- **主标签页导航可自定义**：后台可对 9 个分类重排序、改名、显隐。
- **新增「复原默认外观」**：一键还原上述所有外观设置（不影响网址数据与 SearXNG 设置）。
- **存储与接口**：全部走 `config.json` 的 `site` 字段；前端 `GET /api/settings`（公开）下发，`PUT /api/settings` 保存（鉴权），`POST /api/site/reset` 复原；无后端模式回退默认值。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.2`（或 `:latest`）。数据卷不变，可从 3.0.1 直接升级。

---

## V3.0.1 更新（2026-08-14）· 站点图标本地缓存 + 首页 404 修复

本版本重点解决「每次打开要导图标有点慢」与重装后首页 404 两个问题：

- **站点图标服务端本地缓存（核心提速）**：favicon 由「每次打开逐张远程抓取」改为服务端落盘缓存（`data/favicons/`），首次抓取后秒回；启动时自动预抓取、导入/重置/刷新均触发补齐。实测首次抓取 ~9s → 二次命中缓存 0.002s，提速数千倍。
- **后台「刷新图标」按钮**：新增 `POST /api/favicons/refresh` 强制重抓全部图标，后台「设置」旁一键触发，图标异常/不显示时可手动重建缓存。
- **修复重装后首页 404**：根因是 Dockerfile 构建漏 `COPY index.html`，镜像内无首页文件 → 访问 `/` 返回 404；已补回，重装必须重建镜像方能生效。
- **版本统一**：`server.js` 运行时 `VERSION`、`package.json`、根/构建 `Dockerfile` LABEL、`docker-compose.yaml` 镜像标签、`manifest`、`admin.html` 角标、`index.html` 页脚统一为 `3.0.1`。
- **FPK 离线包**：`qiyi-nav_3.0.1.fpk` 镜像标签升 `3.0.1` 强制飞牛重建，避免旧镜像缓存导致修复不生效。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.1`（或 `:latest`）。数据卷不变，可从 3.0.0 直接升级。

---

## V3.0.0 更新（2026-08-15）· 后台框架全新重构

本版本对后台管理界面做了框架级全新重构，并同步升级版本标识至 3.0.0：

- **后台框架重构**：登录页改为渐变背景 + 品牌区；顶部应用栏（毛玻璃 sticky）；左侧分类侧边栏新增过滤搜索；主区统计精简为 4 张汇总卡（链接总数/分类数/已标记/当前视图）；工具条分组；编辑表单改为右侧滑出抽屉。
- **排版与交互**：统一设计系统（间距/圆角/阴影/字体层级）、卡片与按钮微交互 hover lift、空状态、表单 focus 态、深色模式增强、窄屏响应式（侧栏横排 / 表格卡片化）。
- **功能不变**：前台布局与网格不变；后台原有功能（favicon、浏览器书签导入、批量移动、拖拽排序、自动获取、改密、重置、SearXNG 集成设置）全部保留。
- **版本统一**：`server.js` 运行时 `VERSION`、`package.json`、根/构建 `Dockerfile` LABEL、`docker-compose.yaml` 镜像标签、`manifest`、`admin.html` 角标、`index.html` 页脚统一为 `3.0.0`。
- **FPK 离线包**：`qiyi-nav_3.0.0.fpk` 镜像标签升 `3.0.0` 强制飞牛重建，避免旧镜像缓存。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:3.0.0`（或 `:latest`）。数据卷不变，可从 2.x 直接升级。

---

## V2.3.0 更新（2026-08-14）· 登录加固 + 后台排版优化 + 版本统一

本版本修复后台登录体验、优化后台排版，并将全平台版本标识统一为 2.3.0：

- **后台登录加固**：登录按钮增加「登录中…」加载态与禁用保护，新增网络错误与请求失败明确提示，杜绝「输密码没反应」的观感。
- **后台排版优化**：登录卡片顶部强调条 + 入场动效、容器加宽、统计卡片 hover 悬浮、表格行平滑过渡、仪表盘淡入动效。
- **版本统一**：`server.js` 运行时 `VERSION`、`package.json`、根/构建 `Dockerfile` LABEL、`docker-compose.yaml` 镜像标签、`manifest`、`admin.html` 角标、`index.html` 页脚统一为 `2.3.0`；修正根 `Dockerfile` 遗留的默认密码 `admin123`→`admin` 与 `index.html` 页脚幽灵版本 `V13.6`。
- **FPK 离线包**：`qiyi-nav_2.3.0.fpk` 镜像标签升 `2.3.0` 强制飞牛重建，避免旧镜像缓存导致重装不生效。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:2.3.0`（或 `:latest`）。数据卷不变，可从 2.1.0 / 2.2.0 直接升级。

---

## V2.1.0 更新（2026-08-12）· SearXNG 综合搜索

本版本在 2.0.0 基础上新增**版头综合搜索（SearXNG 元搜索引擎集成）**：

- **版头集成**：搜索框新增「综合」引擎（SearXNG），结果以内嵌浮层呈现，可选新标签页打开官方页。
- **服务端代理**：`/api/search` 由 Node 后端代理到 SearXNG 的 JSON 接口，避免跨域、隐藏实例地址、统一入口。
- **综合版一键部署**：`docker-compose.yml` 内置 `searxng` 服务，与导航站同容器编排，`SEARXNG_URL` 自动指向 `http://searxng:8080`。
- **后台可配置**：`/admin.html` 新增「集成设置」，可开关 SearXNG、改实例地址、设默认引擎、控制新标签页打开，配置持久化到 `data/config.json`。
- **配置 / 文档**：新增 `searxng/settings.yml`（开启 JSON 格式、关闭频率限制）；`README.md`、`FNOS_DEPLOY.md`、`PRODUCT.md` 增补综合版说明。

> 镜像标签：`ghcr.io/<你的用户名>/qiyi-nav:2.1.0`（或 `:latest`）。数据卷不变，可直接从 2.0.0 升级。

---

## 版本

- 版本号：`v2.0.0`
- 日期：2026-08-12
- 协议：MIT

## 这个版本做了什么

把原纯静态 `hao123` 导航站升级为**可自托管的完整产品**：

- **真正的后台管理**：`/admin.html` 支持全分类链接增删改查、搜索筛选、批量删除、导入 / 导出、改密码、一键重置。
- **零依赖 Node 后端**：`server.js` 只用 Node 内置模块，无需 `npm install`，`node server.js` 即跑。
- **服务端持久化**：数据存 `data/links.json`，多设备 / 多浏览器共享，修掉旧版 localStorage 不可跨端、密码可绕过的硬伤。
- **Docker / 飞牛 FNOS 部署**：`Dockerfile`（node:18-alpine）+ `docker-compose.yml`，挂载 `/app/data` 持久化。
- **在线部署（GHCR 自动镜像）**：推 GitHub 后 Actions 自动构建镜像推到容器仓库，飞牛 / 任意 Docker 环境**免源码拉取**部署。
- **版本升级机制**：`git tag` 触发镜像更新，飞牛点「更新」即升级，数据卷不丢。
- **安全加固**：静态白名单（源码不泄露）、目录穿越防护、密码哈希 + HttpOnly Cookie、写接口未授权 401。

## 部署方式

### 方式一：从镜像仓库在线部署（推荐，飞牛 / 任意 Docker）

镜像地址（替换 `<你的用户名>` 为实际 GitHub 用户名）：

```
ghcr.io/<你的用户名>/qiyi-nav:2.0.0
# 或最新版
ghcr.io/<你的用户名>/qiyi-nav:latest
```

- 飞牛 FNOS：见 `FNOS_DEPLOY.md`「方式 C」，填镜像地址 + 映射端口 `1315` + 挂载 `/app/data` 即可。
- 端口：`1315`；后台：`/admin.html`；默认密码 `admin`（请立即修改）。

### 方式二：源码部署

```bash
git clone https://github.com/<你的用户名>/qiyi-nav.git
cd qiyi-nav
# 本地
node server.js
# 或 Docker
docker compose up -d --build
```

## 升级指引

1. 修改代码后打 tag：`git tag v2.0.1`
2. 推送：`git push --tags`（触发 Actions 自动构建 `:2.0.1`）
3. 飞牛容器点「更新」重拉镜像即可；数据卷保留不丢。
4. 回滚：指定旧 tag（如 `:2.0.0`）更新容器。

## 默认凭据（部署前务必修改）

- 管理员密码：`admin123`
- 环境变量：`ADMIN_PASSWORD`、`SESSION_SECRET`（见 `.env.example`）

## 配套文档

- `README.md` — 部署 / 运维总文档
- `FNOS_DEPLOY.md` — 飞牛 FNOS 专属部署指南（含在线部署与升级）
- `PRODUCT.md` — 产品说明（功能 / 架构 / 安全 / FAQ）

## 资源文件

- 源码包：GitHub 自动生成的 Source code (zip / tar.gz)
- 发布包（可选）：`qiyi-nav-v2.0.0-release.tar.gz`（已排除运行期私有数据）

## 3.2.34 (2026-08-20)

修复 `updateTimeDisplay` 与 `formatTime` 双重追加「周X」，导致「周四 周四 第34周」重复；页首压缩 `.header` padding / min-height / LOGO / col-center / 字号，垂直空白显著减少。
