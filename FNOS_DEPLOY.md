# 飞牛 FNOS 部署指南（奇易智能导航 V3.0.1）

本文专为你这台 **飞牛私有云（fnOS）** 上的 Docker 部署整理，照着做即可跑起来。

> 镜像为**零依赖**纯 Node 应用，构建时只需从源码 `docker build`，无需 `npm install`，镜像很小（约 120MB，主要是 Node 运行时）。

> **关于官方 `.fpk` 安装包**：V2.0.0 的 `qiyi-nav_2.0.0.fpk` 已把后端源码与 Dockerfile **内置进包内**（`app/build/`），安装时飞牛直接本地离线构建，默认**不需要 GitHub、不需要 GHCR、没有占位符**。下方「方式 C（拉 GHCR 镜像）」仅当你想多设备统一版本时才用。

---

## 〇、部署前的准备

1. 把整个 `hao123` 文件夹传到飞牛的存储里（用飞牛「文件管理」WebDAV / SMB / 上传都行）。
   建议放到：`/vol1/1000/docker/qiyi-nav/`
   （即文件夹里应直接包含 `Dockerfile`、`docker-compose.yml`、`server.js`、`js/`、`css/`、`admin.html` 等）
2. 在该目录下先建一个空文件夹 `data`，用于存放导航数据（持久化）：
   `/vol1/1000/docker/qiyi-nav/data`
3. 记下飞牛的内网 IP（在飞牛「系统」→「网络」里看，比如 `192.168.1.20`）。

---

## 一、方式 A：Docker「项目 / Compose」（⭐最推荐，一键构建+运行）

飞牛 Docker 应用支持 compose 项目，最适合本项目（因为镜像要从源码构建）。

1. 打开飞牛「Docker」应用 → 进入 **项目（Compose）** 页。
2. 点击「**新建项目**」：
   - 项目名称：`qiyi-nav`
   - 路径/目录：选择 `/vol1/1000/docker/qiyi-nav`（含 docker-compose.yml 的那个目录）
   - 若飞牛要求上传/粘贴 compose 内容，直接粘贴本目录里的 `docker-compose.yml` 内容即可。
3. **重要：先改密码再构建**
   打开 `docker-compose.yml`，把这两行改成你自己的：
   ```yaml
   - ADMIN_PASSWORD=你的强密码            # ⚠️ 默认 admin，建议改成你的强密码
   - SESSION_SECRET=一串随机长字符串      # ⚠️ 随便打一长串，越乱越好
   ```
4. 勾选「**创建后启动**」，点击「**创建**」。
   飞牛会自动执行 `docker compose up -d --build`：拉取 `node:18-alpine` → 构建镜像 → 启动容器。
5. 在「容器」列表里看到 `qiyi-nav` 状态为「运行中」、`healthy`（健康检查通过）即成功。

> 若构建卡在拉取 `node:18-alpine`，说明访问 Docker Hub 慢。去飞牛 Docker 设置里配置**镜像加速源**（如 `https://hub-mirror.c.163.com` 或 `https://docker.1panel.dev`），再重试。

---

## 二、方式 B：手动构建镜像 + 创建容器（不用 compose）

不想用 compose 时，分步来也行：

### 第 1 步：构建镜像
1. 飞牛「Docker」→「**镜像**」→「**构建**」。
2. 构建上下文目录选择 `/vol1/1000/docker/qiyi-nav`。
3. Dockerfile 名称默认 `Dockerfile`（不用改），点击「构建」。
4. 构建完成后镜像名为 `qiyi-nav:latest`（可自定义）。

### 第 2 步：创建并运行容器
1. 飞牛「Docker」→「**容器**」→「**创建容器**」。
2. 镜像选刚构建的 `qiyi-nav`。
3. **端口**：容器端口 `1315` → 宿主机端口 `1315`（冲突就改宿主端口，如 `1316`）。
4. **目录/存储**：
   - 添加绑定挂载：宿主机目录 `/vol1/1000/docker/qiyi-nav/data` → 容器目录 `/app/data`
   - 权限设为「**读写**」。
5. **环境**（点「添加环境变量」）：
   | 名称 | 值 |
   | --- | --- |
   | `TZ` | `Asia/Shanghai` |
   | `PORT` | `1315` |
   | `ADMIN_PASSWORD` | 你的强密码 |
   | `SESSION_SECRET` | 随机长字符串 |
   | `DATA_DIR` | `/app/data` |
6. **重启策略**：选「**容器退出时总是重启**」（= unless-stopped）。
7. 点击「**创建**」并启动。

---

## 三、访问与初始化

- 前台导航： `http://<飞牛内网IP>:1315/`
  （如 `http://192.168.1.20:1315/`）
- 后台管理： `http://<飞牛内网IP>:1315/admin.html`

首次启动会**自动**从内置种子初始化 310 条默认链接（即使 `data` 目录是空的也没问题），无需手动导入。

> 默认管理员密码是你在 `ADMIN_PASSWORD` 里设的值（若没改则是 `admin`）。
> **登录后台后第一件事：点「改密码」换成你自己的强密码。**

> **用 FPK 安装包（推荐）**：宿主端口默认固定为 **1315**，即 `http://<飞牛内网IP>:1315/`；后台 `http://<飞牛内网IP>:1315/admin.html`，密码 `admin`。安装包**无安装向导、无需填写任何项**，一键即可。若之前装过旧版导致密码不对，容器启动会自动把旧配置重置为默认（自愈），无需手动删文件。

---

## 四、数据备份与迁移

数据全在宿主机的 `data` 目录（`/vol1/1000/docker/qiyi-nav/data`），包含：
- `links.json` —— 你维护的导航链接
- `config.json` —— 密码哈希等配置
- `seed.json`  —— 默认种子副本

备份：直接用飞牛「文件管理」把 `data` 文件夹打包下载即可。
迁移到新机器：把整个 `hao123` 目录（含 `data`）拷过去，按上面步骤重新构建运行即可。

也可在后台点「**导出**」下载一份 JSON 备份，重装后用「导入」恢复。

---

## 五、外网访问（可选）

想在手机流量下也能用，任选其一：

1. **飞牛自带远程访问**：飞牛「系统」→「远程访问」开启后再做端口映射/隧道（最省事）。
2. **DDNS + 路由器端口转发**：把宿主端口（如 1315）从路由器转发到飞牛 IP，配合域名访问。
3. **反向代理 + HTTPS**：用 Nginx/Caddy 反代到容器，并申请证书。
   ⚠️ 后台登录走密码，**强烈建议 HTTPS**，否则密码明文传输。

---

## 六、常见问题

| 现象 | 原因 / 解决 |
| --- | --- |
| 容器一直「重启中」或起不来 | 多半是 `data` 目录没创建或没读写权限。确认 `/vol1/.../data` 已建且容器挂载为读写。 |
| 访问 `IP:端口` 显示空白导航 | 正常首次会拉种子；若仍空白，进后台点「重置默认」即可恢复 310 条。 |
| 端口被占用（创建容器报错） | 把宿主端口改成别的（如 1316），容器端口保持 1315。 |
| 构建卡在拉取 node 镜像 | 配置飞牛 Docker 镜像加速源后重试。 |
| 忘记后台密码 | 删除 `data/config.json` 并重启容器，密码回到 `ADMIN_PASSWORD` 的值。 |
| 升级新版 | 重新上传源码 → 重新「构建」镜像 → 重启容器（数据在 `data` 卷里不会丢）。 |

---

## 七、与通用 Docker 部署的区别

- 存储路径用飞牛的 `/vol1/1000/...`（普通 Linux 是 `/opt` 或 `~/`）。
- 飞牛 Docker 默认以 **root** 跑容器，挂载卷权限无障碍，无需额外设 `user`。
- 其余（镜像、端口、API、后台）与 `README.md` 一致。

---

## 八、方式 C：从镜像仓库拉取部署（⭐在线部署，免传源码）

如果你已经把项目推到 GitHub 并开启了自动构建（见 `README.md` 第九节），飞牛**不用传源码、不用构建**，直接拉镜像就能跑：

1. 镜像地址（把 `<用户名>` 换成你的 GitHub 用户名）：
   ```
   ghcr.io/<用户名>/qiyi-nav:latest
   ```
2. 飞牛「Docker」→「**容器**」→「**创建容器**」。
3. 镜像名称填上面的地址。
4. 端口：容器 `1315` → 宿主 `1315`（冲突改宿主端口）。
5. 目录：宿主机 `/vol1/1000/docker/qiyi-nav/data` → 容器 `/app/data`（读写）。
6. 环境：同「方式 B」的 5 个变量（`TZ` / `PORT` / `ADMIN_PASSWORD` / `SESSION_SECRET` / `DATA_DIR`）。
7. 重启策略：容器退出时总是重启。
8. 创建并启动。

> **私有镜像拉取**：若 GitHub 仓库/Packages 设为 Private，飞牛拉取会报 401。
> 解决：飞牛「Docker」→「**注册表/镜像源**」添加 GHCR 登录（用户名=GitHub 用户名，密码=GitHub 新建的 **Fine-grained PAT**，权限勾 `read:packages`），保存后再创建容器即可。
> 想省事就把仓库设为 **Public**，飞牛匿名直接拉。

---

## 九、版本升级与回滚

### 升级（数据不丢）

1. 你在 GitHub 改完代码，打版本 tag 并推送（如 `v3.0.1`），Actions 自动出 `:latest` + `:3.0.1`。
2. 飞牛里对 `qiyi-nav` 容器点「**更新**」→ 重新拉取 `latest` → 重建（挂载的 `data` 卷原样保留，导航数据/密码都在）。
3. 想全自动：在飞牛再部署一个 `watchtower` 容器，监控 `qiyi-nav`，镜像一更新自动重启。

### 回滚

「更新容器」时把镜像标签换成旧版即可，例如 `ghcr.io/<用户名>/qiyi-nav:2.0.0`，`data` 卷不变，链接数据不受影响。

### FPK 离线包如何升级并重构建镜像 ⭐

飞牛「应用中心」安装的 `.fpk` 是**本地离线构建**：镜像不是预先推好的，而是在安装/重装时由包内 `app/build/` 的 Dockerfile 现场 `docker build` 出来。因此：

- **重装（卸载旧版 → 安装新版 `.fpk`）会重建镜像**：飞牛会重新跑构建，用新版 Dockerfile（`COPY index.html` 已在 3.0.1 补回）。这是让 404 等构建类修复**真正生效**的唯一途径。
- **仅点容器「更新」不会重构建**：如果镜像标签没变（比如还停留在 `qiyi-nav:3.0.0`），飞牛只会重拉同名镜像，旧缓存镜像仍可能被复用，构建类修复不会生效。**要让修复落地，必须重新安装（重装）新版本 `.fpk`。**
- 每次发版我们都刻意把镜像标签升一档（`3.0.0` → `3.0.1` → …），`docker-compose.yaml` 里 `image: qiyi-nav:3.0.1` 随之更新，重装即强制新构建，避免复用旧镜像。
- `data` 卷独立挂载，重装应用**不会动你的导航数据/密码**，放心重装。

> 一句话：**构建类修复（如首页 404）= 重装新版 FPK；配置/链接类改动 = 后台操作或更新容器即可。**

### 升级失败怎么办

- 先看飞牛容器日志（Actions 是否构建成功、GHCR 是否有该 tag）。
- 确认飞牛拉取的是 `latest`（私有镜像需配 GHCR 登录，见第八节）。
- `data` 卷独立挂载，任何时候删容器重建都不会丢数据；最坏情况删容器重拉镜像即可。

---

## 十、SearXNG 综合版（版头综合搜索）

导航站版头「综合」引擎由 [SearXNG](https://github.com/searxng/searxng) 提供，部署时**一起把 SearXNG 也跑起来**就是「综合版」。

### 怎么部署综合版

`docker-compose.yml` 已经写好两个服务：`qiyi-nav`（导航）和 `searxng`（搜索）。在飞牛用「方式 A：Docker 项目 / Compose」导入本目录即可一键起两个容器，不用额外操作。

- 导航站：`1315`
- SearXNG：`8081`（网页界面，可单独访问）
- 两者通过容器内部网络互联（`qiyi-nav` 用 `SEARXNG_URL=http://searxng:8080` 调用它）

### 只想用导航、不要 SearXNG？

编辑 `docker-compose.yml`，删掉 `searxng:` 这一段，并删掉 `qiyi-nav` 里的 `SEARXNG_URL` 和 `depends_on` 两行即可。版头「综合」引擎会提示不可用，其他搜索引擎正常。

### 已有自己的 SearXNG？

不用再部署。登录后台 `/admin.html` → 「集成设置」→ 把「SearXNG 实例地址」填成你已有的地址（如 `http://192.168.1.10:8081`）即可接入。

### 配置要点

- 配置文件在 `searxng/settings.yml`：已开启 JSON 输出（供导航代理）、关闭频率限制（个人用）。
- 公网暴露 SearXNG 网页界面建议设访问限制；导航站代理接口本身不暴露实例地址。
- SearXNG 首次启动可能稍慢（约 10~30 秒），飞牛健康检查已留 `start_period: 30s`。
